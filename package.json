{
  "name": "satu-jam-saja",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx kill-port --port 3000 && next dev",
    "build": "next build",
    "start": "next start",
    "dev:kill": "npx kill-port --port 3000 && npm run dev"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "kill-port": "^2.0.1",
    "typescript": "latest"
  }
}
